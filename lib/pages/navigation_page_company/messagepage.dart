import 'package:flutter/material.dart';

class MessagePageCompany extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("MsgPage"),
      ),
    );
  }
}
