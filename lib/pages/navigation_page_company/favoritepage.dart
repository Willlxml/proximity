import 'package:flutter/material.dart';

class FavoritePageCompany extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("FavoritePage"),
      ),
    );
  }
}
